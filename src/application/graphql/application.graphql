scalar Date
type Application{
   id:ID!
  candidateId: String!
  jobId: String!
  referredBy: String
  status: String!
  appliedDate: Date
  candidate:Candidate!
  referrer:Employee
  job:Job
}


type Query {
  getAllReferred:[Application!]
}